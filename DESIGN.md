# Design

Дизайн-код коммерческого раздела `/dostavka/` («Щебень-Урал»). Записан
с собранного результата, а не с замысла: всё ниже можно найти в
`dostavka/assets/dostavka.css` и в шаблонах `dostavka-src/templates/`.

Информационная половина сайта (бренд «НАШДОМ», 24 страницы) **этот код
пока не использует** и живёт на старой вёрстке. Это известное расхождение,
а не упущение записи.

## Мир: «Рассев»

Сайт устроен как прибор ситового анализа, а не как брошюра о доставке.
Ситовой анализ — стопка сит с ячейкой от мелкой к крупной: материал
просыпается вниз, на каждом сите остаётся своя фракция.

Почему именно это. Фракция — первое, что человек обязан выбрать, и первое,
чего он не понимает (подтверждено семантикой: «какая фракция под фундамент»,
«чем отсыпать участок»). Прибор, который сортировку делает физически,
объясняет её быстрее любого абзаца.

Отвергнутый по умолчанию ход ниши: тёмный герой с фотографией самосвала,
телефон в правом верхнем углу, три карточки доверия, прайс ниже. Раздел
раньше выглядел ровно так.

### Три правила, из которых выведено остальное

1. **Сетка структурна.** Шаг ячейки `--cell` везде вычислен из настоящего
   верхнего предела фракции: `cell = 4px + мм × 0,75`. Линейный масштаб дал
   бы песку ячейку в 2px, невидимую; поправка поднимает мелкий конец.
   Сетка, нарисованная наугад, превращает прибор в обои — тогда мира нет.
2. **Четыре поверхности, у каждой роль.** Сталь несёт крупные поля, эмаль —
   содержимое, графит — только разлиновку, латунь — только действие.
   Пятого материала нет.
3. **Цифра набрана измерительным знаком.** Все цифры сайта идут
   моноширинным начертанием: число здесь — показание прибора, а не слово.

## Токены

### Цвет

| Токен | Значение | Роль |
|---|---|---|
| `--d-steel` | `#1b2530` | корпус: шапка, герой, тёмные секции, подвал |
| `--d-steel-2` | `#243039` | наведение на стали, плашка подписи |
| `--d-steel-3` | `#33424e` | линия на стали |
| `--d-enamel` | `#eef1ec` | фон страницы |
| `--d-enamel-2` | `#e4e8e1` | утопленный лоток, крошки |
| `--d-paper` | `#f7f8f5` | лоток содержимого: таблицы, формы, карточки |
| `--d-ink` | `#141a20` | основной текст |
| `--d-ink-2` | `#4a565f` | вторичный текст |
| `--d-graph` | `#5d7b88` | **только линии**, буквами не набирается |
| `--d-rule` / `--d-rule-2` | `#ccd3cd` / `#b6bfb8` | разлиновка и обрез лотка |
| `--d-wire` / `--d-wire-dark` | `#76837b` / `#54666f` | проволока сита на светлом / на стали |
| `--d-brass` | `#d9a548` | **только действие**: заливка кнопки, обод сита |
| `--d-brass-h` | `#c4922f` | наведение |
| `--d-brass-text` | `#7d570c` | латунь, годная для букв на светлом |
| `--d-on-steel` / `--d-on-steel-2` | `#e9ede9` / `#a3b0b8` | текст на стали |
| `--d-rust` / `--d-rust-bg` | `#a8442a` / `#f6ece7` | **только предупреждение** |

Контраст посчитан по пикселям, не на глаз, и записан комментариями рядом
с токенами. Опорные значения:

- ink на эмали **15,38**; ink-2 на эмали **6,61**
- on-steel на стали **13,11**; on-steel-2 на стали **6,99**
- brass на стали **6,96**; brass-text на эмали **5,69**
- **steel на латуни 6,96 — это единственные чернила, допустимые на кнопке.**
  Белый на латуни даёт **2,23** и запрещён без исключений.
- graph на эмали **3,96**: хватает линии, не хватает буквам. Отсюда правило
  «графит только разлиновка».
- wire на эмали **3,48**: ячейка несёт смысл, поэтому обязана проходить
  порог 3:1 для нетекстовой графики.

### Шрифт

- **Golos Text**, вариативный 400–900. Гротеск, нарисованный под кириллицу,
  а не с латиницы с достроенной кириллицей. Два файла: кириллица 21,5 КБ,
  латиница подрезана до реально встречающихся символов, 23,6 КБ.
- **JetBrains Mono**, подрезанный до `U+0030-0039, U+002D, U+00B2-00B3,
  U+00D7` — 9,4 КБ. Подключён **первым** в стеке с узким `unicode-range`,
  поэтому перехватывает ровно эти знаки; буквы и прочая пунктуация
  остаются в Golos.
  `size-adjust: 95.9%` — у Golos высота прописных 0,700 em, у JetBrains Mono
  0,730; цифры маюскульные и равняются по прописным, без поправки строка
  «плывёт».

Шкала: `h1` clamp 30–46px, `h2` clamp 22–29px, `h3` 19px, тело 17px/1.62.
Трекинг заголовков −0,03…−0,035em, `text-wrap:balance`. Мера текста 70ch.

### Геометрия и глубина

Один радиус `--r: 2px` на лотки, ноль на линиях. Скруглений больше нигде —
именно этим измерительный корпус отличается от карточки-плитки.

Внутренняя тень у утопленного, внешняя (`--sh-lift`, `--sh-lift-2`) только
у того, что действительно лежит сверху: всплывающее окно.

## Компоненты

- **`.d-sieve` — стопка сит.** Главный объект. На хабе, в первом экране.
  Четыре строки: ячейка 104px в масштабе, три нарисованных зерна шириной
  `1,35 × --cell` (заведомо крупнее отверстия — потому и не прошли),
  фракция, назначение, цена за куб. Строка кликается целиком через
  растянутый `::after` у `.d-sieve-frac`. Данные — `SIEVE` в
  `dostavka-src/data/prices.py`, цены оттуда же, откуда прайс.
- **`.d-hero-panel` — боковое сито.** На остальных страницах, ≥900px.
  Ячейка страницы крупным планом плюс подпись, называющая фракцию словами
  (`HERO_FRAC`). Ниже 900px панель скрыта, вместо неё полоса сита по нижней
  кромке героя высотой `--cell + 12px` — не меньше двух рядов, иначе полоса
  читается забором.
- **`.d-grid` / `.d-card` — ведомость.** Пары «наименование — значение»
  разлиновкой, как строка в паспорте материала. Плиток из иконки, заголовка
  и абзаца в этом мире нет.
- **`.d-cat` — каталог.** Лотки на общей разлиновке (`gap:1px` по фону),
  цена крупным измерительным знаком сразу.
- **`table` — протокол замера.** Стальная шапка, тонкая разлиновка, столбец
  значений весом 650. Подпись `position:sticky; left:0`, потому что ширина
  caption равна ширине таблицы, а таблица на узком экране шире экрана.
- **`.d-callout` — утопленный лоток.** Внутренняя тень и рамка по кругу.
  Цветной полосы слева нет.
- **`.d-btn`.** Латунь + стальные чернила. Контурный вариант меняет обвод.
- **Знаки.** Один язык: маркер списка — латунный квадрат под 45°, стрелка
  каталога и раскрывашка FAQ — два штриха в 2px. Юникодных значков нет.

## Движение

Один поставленный момент: сита на хабе заполняются сверху вниз
(`clip-path: inset(0 100% 0 0)` → `0`, 620 мс, `cubic-bezier(.16,1,.3,1)`,
шаг 70 мс). Анимируется только ячейка — строки, цены и ссылки видны сразу
и от анимации не зависят. Всё остальное движение — состояния наведения.
`prefers-reduced-motion` гасит и то, и другое.

## Поверхности браузера

Взяты в мир, а не оставлены системными: выделение (латунь + стальные
чернила), каретка, полоса прокрутки, кольцо фокуса. На стальных
поверхностях кольцо переключается с `--d-brass-text` на `--d-brass`.

## Запреты

- Белые буквы на латуни.
- Графит буквами.
- Плашка-надзаголовок над заголовком. Убрана со всех шести шаблонов
  и не возвращается.
- Цветная полоса толще 1px слева у карточек, выносок и списков.
  Латунная скоба в 3–4px допустима **только** как обод сита: у стопки,
  у боковой панели, у логотипа, у стального блока связи.
- Плитки «иконка + заголовок + абзац» как структура страницы.
- Скругления крупнее 2px.
- Сетка, шаг которой не выведен из настоящего размера фракции.
- Селектор под класс, которого нет в разметке. Проверка соответствия
  классов в обе стороны — группа 19 в `audit/_verify.py`.

## Проверка

Каждая правка проходит `python3 audit/_verify.py` (66 страниц,
19 групп проверок). Правило проекта: новая проверка подтверждается
повторным внесением бага, который она должна ловить.

Замеры первого экрана на 390×844: на хабе первая цена на **476px**
(в предыдущей версии таблица цен начиналась на 804px, то есть за экраном).
