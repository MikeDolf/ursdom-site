# Design

Дизайн-код коммерческого раздела `/dostavka/` («Щебень-Урал»). Записан
с собранного результата: всё ниже можно найти в
`dostavka/assets/dostavka.css` и в шаблонах `dostavka-src/templates/`.

Информационная половина сайта (бренд «НАШДОМ») этот код не использует.

## Решение сентября 2026: схема блоков по образцу владельца

Владелец прислал страницу своего второго проекта (гео-страница «перегной
купить в Ревде») и попросил **тот же порядок и то же устройство блоков во
всём разделе**. Это заменило прежний мир «Рассев», где страница была
прибором ситового анализа: стальной герой без фото, ведомости вместо
карточек, радиус 2px, запрет на плитки с иконками.

Что взято у образца: полосы секций, заголовок секции с чертой, белые
скруглённые карточки с мягкой тенью, иконки у гарантий и преимуществ,
лента цифр, карточки шагов, форма на белой карточке в тёмной полосе,
большая лента фото доставок, машины с фото, плашки-ссылки.

Что **не** взято: палитра. Образец зелёный, это бренд fanline.su, а темы
и бренды двух сайтов разделены по требованию владельца. Материалы
остаются прежними: сталь, эмаль, латунь, Golos Text, измерительные цифры.

## Порядок блоков

Коммерческая страница (город, материал, главная раздела, рубрика):

1. Первый экран: настоящее фото на фоне под затемнением, H1, подзаголовок,
   цена «от» в латунной плашке, плашка «оплата после выгрузки», кнопки
   MAX и «Узнать цену с доставкой». WhatsApp убран по решению владельца:
   туда никто не пишет.
2. Сколько стоит: карточки цены машины по объёмам (города) или витрина
   материалов и готовые объёмы (товарные страницы).
3. Калькулятор: поля слева, тёмная панель с итогом справа.
4. Наши доставки и техника: лента крупных фото.
5. Быстрая заявка: тёмная полоса, текст слева, короткая форма справа.
6. Наши гарантии: тёмная полоса, шесть карточек с иконками, под ней лента
   доверия (круглосуточно, без предоплаты, 5-20 м³, реквизиты).
7. Почему заказывают у нас: шесть карточек, под ними лента цифр.
8. Как заказать: четыре карточки с номером шага.
9. Оставить заявку: тёмная полоса, форма на белой карточке.
10. Проще написать? Мы в MAX: карточка с кнопками.
11. Доставка в город (три карточки фактов), машины с фото.
12. Справочная часть: текст, таблицы, расчёты. Абзацы в колонку 70ch,
    таблицы и карточки на всю ширину полосы.
13. Полный прайс, плашки-ссылки, частые вопросы.

Статьи (`longread.j2`) — режим чтения: сначала текст целиком, потом
продающий хвост с пункта 4. Страница «спасибо» и политика — простые
текстовые страницы без продающих блоков.

Блоки продающего хвоста помечены `data-ui`. Проверка похожести страниц
(`audit/_verify.py`, группа 17) их не сравнивает: они одинаковы на всех
страницах по замыслу, как шапка и подвал.

## Токены

### Цвет

| Токен | Значение | Роль |
|---|---|---|
| `--d-steel` | `#1b2530` | корпус: шапка, герой, тёмные секции, подвал |
| `--d-steel-2` | `#243039` | наведение на стали, плашка подписи |
| `--d-steel-3` | `#33424e` | линия на стали |
| `--d-enamel` | `#eef1ec` | фон страницы |
| `--d-enamel-2` | `#e4e8e1` | утопленный лоток, крошки |
| `--d-paper` | `#f7f8f5` | лоток содержимого: таблицы, формы, карточки |
| `--d-ink` | `#141a20` | основной текст |
| `--d-ink-2` | `#4a565f` | вторичный текст |
| `--d-graph` | `#5d7b88` | **только линии**, буквами не набирается |
| `--d-rule` / `--d-rule-2` | `#ccd3cd` / `#b6bfb8` | разлиновка и обрез лотка |
| `--d-wire` / `--d-wire-dark` | `#76837b` / `#54666f` | проволока сита на светлом / на стали |
| `--d-brass` | `#d9a548` | цены, черта заголовка, обод сита |
| `--d-go` / `--d-go-h` | `#15803d` / `#116932` | **только действие**: кнопки MAX и отправки формы, белый текст 5,0 и 6,5 |
| `--d-brass-h` | `#c4922f` | наведение |
| `--d-brass-text` | `#7d570c` | латунь, годная для букв на светлом |
| `--d-on-steel` / `--d-on-steel-2` | `#e9ede9` / `#a3b0b8` | текст на стали |
| `--d-rust` / `--d-rust-bg` | `#a8442a` / `#f6ece7` | **только предупреждение** |

Контраст посчитан по пикселям, не на глаз, и записан комментариями рядом
с токенами. Опорные значения:

- ink на эмали **15,38**; ink-2 на эмали **6,61**
- on-steel на стали **13,11**; on-steel-2 на стали **6,99**
- brass на стали **6,96**; brass-text на эмали **5,69**
- **steel на латуни 6,96 — это единственные чернила, допустимые на кнопке.**
  Белый на латуни даёт **2,23** и запрещён без исключений.
- graph на эмали **3,96**: хватает линии, не хватает буквам. Отсюда правило
  «графит только разлиновка».
- wire на эмали **3,48**: ячейка несёт смысл, поэтому обязана проходить
  порог 3:1 для нетекстовой графики.

### Шрифт

- **Golos Text**, вариативный 400–900. Гротеск, нарисованный под кириллицу,
  а не с латиницы с достроенной кириллицей. Два файла: кириллица 21,5 КБ,
  латиница подрезана до реально встречающихся символов, 23,6 КБ.
- **JetBrains Mono**, подрезанный до `U+0030-0039, U+002D, U+00B2-00B3,
  U+00D7` — 9,4 КБ. Подключён **первым** в стеке с узким `unicode-range`,
  поэтому перехватывает ровно эти знаки; буквы и прочая пунктуация
  остаются в Golos.
  `size-adjust: 95.9%` — у Golos высота прописных 0,700 em, у JetBrains Mono
  0,730; цифры маюскульные и равняются по прописным, без поправки строка
  «плывёт».

Шкала: `h1` clamp 30–46px, `h2` clamp 22–29px, `h3` 19px, тело 17px/1.62.
Трекинг заголовков −0,03…−0,035em, `text-wrap:balance`. Мера текста 70ch.

### Геометрия и глубина

| Токен | Значение | Роль |
|---|---|---|
| `--d-white` | `#ffffff` | поверхность карточки, ink на белом 17,3 |
| `--r-card` | `14px` | карточки, лента фото, формы, таблицы |
| `--r-ctl` | `9px` | кнопки, поля ввода |
| `--sh-card` | две мягкие тени | всё, что лежит карточкой |
| `--r` | `2px` | остался у мелких элементов прежнего мира |
| `--band-pad` | `clamp(44px, 6.5vw, 80px)` | поле полосы секции |

Полосы: `.d-band` (бумага), `.d-band-tint` (эмаль), `.d-band-dark` (сталь).
Полосы чередуются, тёмная не встаёт рядом с тёмной.

## Компоненты

Все в конце `dostavka-src/templates/_macros.j2`, стили в конце `dostavka.css`.

- `hero2` — первый экран с фото. Снимок `<picture>` под градиентом:
  плотным слева, где текст, прозрачным справа; на телефоне сплошным.
  На главной поверх стоит стопка фракций, затемнение там сплошное.
- `sh` — заголовок секции с латунной чертой 46×3px.
- `d-lotcards` — цена машины карточками: объём плашкой, итог крупно,
  из чего сложен.
- `calculator` — поля и тёмная панель `.d-calc-res` с итогом латунью.
- `deliveries` — лента фото с прилипанием кадра; стрелки добавляет wa.js.
- `quick_band`, `form_band` — тёмная полоса, форма на белой карточке.
  Внутри карточки чернила и кольцо фокуса светлой поверхности.
- `guarantee_band`, `why_band` — карточки с иконкой `d-icard`; на телефоне
  строкой (иконка слева), чтобы шесть карточек не занимали два экрана.
- `steps_band`, `msg_card`, `info_cards`, `trucks`, `chips`.
- `icon` — 15 иконок одним контуром 24×24, штрих `currentColor` 1,8,
  нарисованы в макросе, без библиотек.
- Прежние блоки переведены в систему: витрина цен, каталог, «под
  задачу», таблицы, частые вопросы (карточками), «смотрите также»
  (плашками).

## Что осталось от «Рассева»

- Палитра, шрифты, моноширинные цифры, контраст (таблица выше).
- Стопка фракций `.d-sieve` на главной в первом экране.
- Белые буквы на латуни по-прежнему запрещены: на латуни стальные
  чернила (6,96).

## Запреты

- Белые буквы на латуни.
- Графит буквами.
- Палитра fanline.su в полосах и фонах. Зелёный разрешён только
  заливкой кнопок действия (`--d-go`): так решил владелец в сентябре
  2026, чтобы кнопки выделялись на фоне остального сайта.
- Две тёмные полосы подряд.
- Селектор под класс, которого нет в разметке, и класс без стиля.
  Проверка в обе стороны — группа 19 в `audit/_verify.py`.

## Телефон

Основная доля посетителей с телефона, и раскладка на 390 px проверяется
снимками, а не только аудитом. Правила из ревью сентября 2026:

- **Кнопка MAX в первом экране.** Липкая панель появляется только после
  прокрутки, поэтому на первом экране кнопка обязана быть в потоке
  выше 650 px. На узком экране плашка оплаты уходит под кнопки
  (`order` в `.d-hero2-in`), порядок в разметке прежний.
- **Таблицы в три столбца и больше карточками.** Класс `.d-stack`
  и подписи `data-label` ставит сборка (`stack_tables` в build.py):
  строка становится карточкой «подпись: значение». Свои раскладки
  у калькулятора, прайса машины на городских страницах, ответов
  покупателям и списка городов на главной (`.d-cities`, две колонки).
- **Калькулятор строкой**: подпись слева, сумма у правого края,
  формула мелкой строкой под ними.
- **Частые вопросы**: первые восемь видны, остальные под «Показать
  ещё вопросы». В разметке и FAQPage все.
- **Форма**: обязательны телефон и согласие, имя по желанию;
  `autocomplete` у имени и телефона; пример в поле «Что и куда
  привезти» строится из темы заявки страницы (фильтр `form_example`);
  номер короче 10 цифр не уходит, кнопка блокируется на время отправки.
- Шапка на телефоне 60 px, цели нажатия по-прежнему 48 px.

## Проверка

Каждая правка проходит `python3 audit/_verify.py` и рендер-аудит
`node audit/_ui_audit.mjs` на телефоне и компьютере.
