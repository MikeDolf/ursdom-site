    {
      "@type": "ItemList",
      "name": "{{ itemlist_name }}",
      "itemListOrder": "https://schema.org/ItemListOrderDescending",
      "itemListElement": [
{%- for it in itemlist %}
        { "@type": "ListItem", "position": {{ loop.index }}, "name": "{{ it.name }}"{% if it.url %}, "url": "{{ it.url }}"{% endif %} }{{ "," if not loop.last else "" }}
{%- endfor %}
      ]
    }